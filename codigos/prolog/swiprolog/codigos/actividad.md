# actividad

probar códigos de prolog con swiprolog

1. abra una consola de bash en esta carpeta
2. cargue: `swipl -s ingredientes.pl`
3. consulte:
   1. hay(X).
   2. que_hay(L).
   3. lista_faltantes(sopa).
   4. termine con `halt.`
4. cargue `swipl -s ingredientes.listas.pl`
5. consulte:
   1. lleva(ensalada,L).
   2. faltan_ingredientes_para(sandwich,X).
   3. termine con `halt.`
6. cargar: `swipl -s menu.pl`
   1. consulte:
   2. menu.
   3. pruebe sus opciones dnado un numero por ejemplo: 1.
   4. slga con 4.
7. cargue consultav2.pl y pruebe:
   1. evaluar.
   2. conteste si./no.
   3. vea el resultado
   4. termine con `halt.`
   5. analice el código con el profesor.